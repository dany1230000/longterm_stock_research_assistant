# 00631L v16.96 settings static history caption

v16.96 makes the account/settings fallback wording clearer.

## Changes

- The compact account page now says `靜態歷史可用` when live backend data is not connected.
- The longer wording is `靜態歷史可用；Live 連線細節在進階`.
- This keeps the account first screen focused on what the user can still use.

## Scope

No repository fallback order, backend endpoint, or static data behavior changed.
This release only updates settings copy and widget coverage.
