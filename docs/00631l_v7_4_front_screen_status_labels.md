# 00631L lab v7.4 front screen status labels

v7.4 cleans up the most visible status chips on the overview and selected ETF
context screens.

## Changed

- Overview status chips now use Chinese labels for core data, holdings,
  intraday NAV, history, frontend mode, backend checks, row counts, and export
  time.
- History count captions now use `筆` instead of `rows`.
- Selected ETF context now uses Chinese labels for history source, row count,
  coverage range, static/catalog/error states, and live NAV scope.
- AI detail status chips now use `分析來源` and `整體狀態`.

## Scope

- No data source changes.
- No parser changes.
- No trading workflow.
- No investment guidance.
