# 00631L lab v7.3 data coverage labels

v7.3 cleans up the advanced data coverage panel so it reads like product UI
instead of internal diagnostics.

## Changed

- `TX live` is now labeled `台指期即時`.
- `ETF catalog` is now labeled `ETF 清單資料`.
- `ETF history` is now labeled `ETF 價格歷史`.
- Detail text now uses Chinese labels for row counts, coverage range, source,
  data time, coverage tiers, and gap state.

## Scope

- No data source changes.
- No parser changes.
- No trading workflow.
- No investment guidance.
