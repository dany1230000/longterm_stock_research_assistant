# 00631L v16.94 history default range label

v16.94 clarifies the first metric in the history/backtest top strip.

## Changes

- The compact top strip now labels the `近1年` chip as `預設`.
- This separates the default one-year view from the editable date range controls below it.
- The history/backtest page keeps the same data, calculations, and chart behavior.

## Scope

No backend, static data, backtest engine, or chart calculation changed.
This release only updates a mobile-first history/backtest label and its widget test.
