# 00631L v6.98 Overview Holdings Digest Badges

## Scope

This release continues first-screen cleanup. The overview holdings digest no
longer uses `DAY / TX / 2330 / MIX` badges as primary visual elements.

## Behavior

- The holdings digest title starts directly with the user-facing section name.
- Digest tiles use `期貨`, `台積電`, and `曝險結構` as the primary labels.
- The underlying values and official snapshot source behavior are unchanged.
- Expanded technical panels may still show compact source badges where useful.

## Validation

- Phone-width widget coverage confirms the digest shows the Chinese labels.
- The same coverage confirms `TX / 2330 / MIX` no longer appear inside the
  first-screen holdings digest strip.
