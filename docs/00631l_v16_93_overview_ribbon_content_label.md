# 00631L v16.93 overview ribbon content label

v16.93 makes the overview data ribbon easier to read.

## Changes

- The compact overview ribbon now labels the official daily snapshot as `內容`.
- The ribbon reads as `內容 / 盤中 / 歷史 / 模式`.
- This keeps the first screen focused on the main data freshness types without short internal badges.

## Scope

No data source, refresh, chart, holdings, or backtest behavior changed.
This release only updates the overview compact ribbon label and widget coverage.
