# 00631L v16.68 AI Daily Readout

This release makes the phone AI first screen more data-driven.

Changes:

- The compact AI insight now uses the daily readout sentence directly.
- The first screen includes holdings date, TX weight, TSMC weight,
  premium/discount state, and history row count.
- Rule-based AI remains the default; no external LLM key is required.
- No data source, parser, or calculation logic changed.

Validation focus:

- The AI first screen shows daily data context instead of generic source copy.
- The full detail panel remains available for source and readiness checks.
- The screen continues to show non-advice wording only.
