# 00631L lab v15.37 compact loading shell

## What Changed

- The first loading screen now uses a shorter market-style status strip.
- The old large loading section card was replaced by a compact source rail:
  `HIS`, `LIVE`, and `AI`.
- The shell still keeps the app frame, quote skeleton, and bottom navigation so
  the page never looks blank while public/static/live data loads.

## Scope

- UI loading-state polish only.
- No source behavior changes.
- No generated static data committed.
