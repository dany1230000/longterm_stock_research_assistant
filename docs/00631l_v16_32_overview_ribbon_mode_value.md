# 00631L v16.32 Overview Ribbon Mode Value

This patch removes internal mode codes from the phone overview ribbon.

Changes:

- `mock` is shown as `示範`.
- `static` is shown as `靜態`.
- `live` is shown as `Live`.

The raw mode names remain available in technical status areas where they are
useful for verification. The first screen now uses product-facing labels.

Scope:

- UI wording only.
- No data source changes.
- No investment guidance.
