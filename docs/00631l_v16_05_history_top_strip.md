# 00631L v16.05 History Top Strip

## Scope

v16.05 improves the phone history/backtest first screen by making the top strip
show the active one-year view more directly.

## UI Behavior

- The compact top strip now shows `期間 1Y` instead of using the first metric for
  only the latest date.
- The latest available date remains visible beside the ETF code.
- Row count, total return, and max drawdown stay in the same compact metric row.
- Date range controls below the strip still allow changing the visible range.

## Data Scope

No historical price calculation changed. Split-adjusted performance data,
backtest inputs, and comparison behavior remain unchanged.
