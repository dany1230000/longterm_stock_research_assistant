# 00631L v16.67 History Date Focus

This release improves the phone history/backtest first screen.

Changes:

- The compact date-range header now shows the primary selected date range only.
- Secondary fields such as sample count, strategy, and cost remain in their
  dedicated controls below the date row.
- No historical data, backtest calculation, or source selection logic changed.

Validation focus:

- History/backtest still defaults to one year.
- Date presets and start/end controls remain visible.
- The compact summary row no longer mixes unrelated parameters into the same
  line.
