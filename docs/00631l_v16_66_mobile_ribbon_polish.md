# 00631L v16.66 Mobile Ribbon Polish

This release keeps the existing data model and app sections, but tightens the
phone first screen.

Changes:

- The overview data ribbon now uses explicit labels: `DAY`, `LIVE`, `HIS`, and
  `MODE`.
- The top app bar and bottom navigation are slightly shorter, keeping the quote
  and one-year chart visible sooner.
- No data source, parser, backtest, position, or AI logic changed.

Validation focus:

- The phone overview still shows quote, chart, source status, and key holdings
  context.
- The ribbon labels are meaningful and do not behave like decorative icons.
- Forbidden trading wording remains blocked.
