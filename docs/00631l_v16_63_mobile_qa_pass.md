# 00631L v16.63 Mobile QA Pass

This QA pass keeps the comparison section consistent across phone and desktop
layouts.

- Phone and wider layouts now both use `ETF 自選比較`.
- Selected comparison captions use `自選組合`.
- Summary text continues to state `不設基準`, so comparison remains a
  user-selected data view rather than a fixed benchmark view.

No data source, backtest, position, or analysis calculation logic changed.
