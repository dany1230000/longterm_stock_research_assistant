# 00631L lab v9.29 overview axis labels

v9.29 improves the first-screen overview sparkline date strip.

## What changed

- The overview chart now labels its date strip as `起點`, `中段`, and `終點`.
- The labels match the history/backtest chart wording from v9.27.
- The chart remains visible on the first screen and is not folded behind an
  expansion panel.

## Scope

This is a phone readability update only. It does not change historical data,
backtest calculations, holdings data, or live backend behavior.
