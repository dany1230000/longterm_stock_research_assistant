# 00631L v16.99 bottom history label

v16.99 makes the bottom navigation read more like a production app.

## Changes

- The combined history/backtest tab now shows `歷史` in the bottom navigation.
- The page still contains backtest controls, results, comparison, and date range tools.
- The in-page title can still use `歷史回測` where the combined function needs to be explicit.

## Scope

No history data, backtest calculation, routing key, or comparison behavior changed.
This release only updates the bottom navigation label and widget coverage.
