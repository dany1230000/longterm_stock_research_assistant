# 00631L lab v3.17 information hierarchy summary

完成日期：2026-06-12

## 完成範圍

- 總覽頁改成更明確的資訊層級：
  - `今日快覽`：保留 AI 簡短摘要與必要資料狀態。
  - `核心數字比較`：把行情 / NAV 與規模 / 歷史放在同一區比較。
  - `資料來源比較`：只列常用來源 `DAY`、`LIVE`、`HIS`、`AI`。
- 將較不常看的 coverage 明細與歷史完整度移到「更多資料狀態」收合區。
- 設定頁的 `進階診斷` 改為預設收合，只有排除 backend、report、export、backup 問題時才展開。
- 移除已被取代的舊首頁表格式元件，避免保留無用 UI 程式碼。

## 資料與範圍

- 沒有新增資料來源。
- 沒有接 TX live。
- 沒有擴大到所有正二。
- 沒有新增投資建議。

## 驗收重點

- 手機第一屏不再被低優先維護資訊占用。
- 同類型資料放在同一區比較，方便掃描。
- technical diagnostics 仍可展開查看，但不干擾一般使用。
